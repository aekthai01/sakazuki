# กฎการทำงานของโปรเจกต์ Sakazuki

เอกสารนี้กำหนดข้อจำกัดและหน้าที่ในการดูแลโปรเจกต์ เพื่อป้องกันความผิดพลาดจากการทำงานผิดทิศทางระหว่าง GitHub, Termux และโฮสต์จริง

## 1. สภาพแวดล้อมของผู้ใช้

- ผู้ใช้ทำงานผ่าน **Termux บนโทรศัพท์ Android เท่านั้น**
- ผู้ใช้ **ไม่มีคอมพิวเตอร์**
- คำสั่งหรือขั้นตอนที่ให้ผู้ใช้รันเองต้องสามารถทำได้จาก Termux บนโทรศัพท์
- ห้ามออกแบบขั้นตอนที่บังคับให้ใช้ Windows, macOS หรือโปรแกรมบนคอมพิวเตอร์ หากมีวิธีทำผ่าน Termux ได้

## 2. หน้าที่ของผู้ช่วย

ผู้ช่วยมีหน้าที่ช่วยดูแลโปรเจกต์ในงานต่อไปนี้:

- แก้ไขโค้ด
- ตรวจสอบและ review โค้ด
- ตรวจสอบ syntax, logic, security และปัญหาที่อาจเกิดขึ้น
- ทำ backup ก่อนงานที่มีความเสี่ยงหรือก่อนเปลี่ยนแปลงสำคัญ
- อัปเดตไฟล์ใน GitHub ตามงานที่ได้รับมอบหมาย
- เตรียมและอัปโหลดไฟล์ลงโฮสต์เมื่อได้รับอนุญาต
- ตรวจสอบความแตกต่างระหว่าง GitHub, backup และโฮสต์
- วิเคราะห์และแก้ไขปัญหาต่าง ๆ ของเว็บไซต์

## 3. กฎสำคัญเกี่ยวกับโฮสต์ Production

- **ห้ามแก้ไขไฟล์บนโฮสต์ก่อนผู้ใช้อนุญาตอย่างชัดเจน**
- ก่อนอนุญาต สามารถทำได้เฉพาะงานแบบ read-only เช่น ดูรายการไฟล์, ดาวน์โหลดสำเนาเพื่อตรวจสอบ, เปรียบเทียบไฟล์ หรืออ่านข้อมูลที่ไม่เปลี่ยนแปลงโฮสต์
- ห้าม upload, overwrite, delete, rename, chmod, deploy หรือสั่งงานใด ๆ ที่ทำให้ไฟล์บนโฮสต์เปลี่ยนแปลง โดยไม่ได้รับอนุญาตก่อน
- หากต้องแก้โฮสต์ ต้องอธิบายก่อนว่าจะเปลี่ยนไฟล์ใดและเพราะอะไร แล้วรอการอนุญาตจากผู้ใช้
- เมื่อได้รับอนุญาตให้แก้โฮสต์ ต้องทำ backup ที่เหมาะสมก่อน หากการเปลี่ยนแปลงนั้นมีความเสี่ยง

## 4. กฎเรื่อง Source of Truth

- ห้ามสมมติว่า GitHub เป็นไฟล์ล่าสุดเสมอ
- ห้ามสมมติว่าไฟล์บนโฮสต์เป็นไฟล์ล่าสุดเสมอ
- ก่อน sync หรือ deploy ต้องตรวจสอบก่อนว่า source ใดคือเวอร์ชันล่าสุดที่ผู้ใช้ระบุ
- ถ้ามีความไม่แน่ใจ ให้เปรียบเทียบไฟล์ก่อน ห้ามเขียนทับอีกฝั่งโดยการคาดเดา
- ทิศทางการ sync ต้องเป็นไปตาม source ที่ผู้ใช้ยืนยันเท่านั้น

## 5. หลักการความปลอดภัย

- ห้ามนำ password, token, secret key, database credential หรือไฟล์ secret ขึ้น GitHub
- ห้ามแสดง secret ใน log หรือข้อความที่ไม่จำเป็น
- ไฟล์ runtime, cache และ user uploads ที่ไม่ควรอยู่ใน GitHub ต้องถูกแยกออกจาก source code
- ก่อน commit หรือ deploy งานสำคัญ ควรตรวจ diff และรายการไฟล์ที่เปลี่ยนก่อนเสมอ

---

กฎชุดนี้ใช้เป็นแนวทางหลักสำหรับการทำงานกับโปรเจกต์นี้ เว้นแต่ผู้ใช้จะสั่งเปลี่ยนแปลงเป็นกรณีเฉพาะอย่างชัดเจน
